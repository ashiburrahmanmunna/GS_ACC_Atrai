@model IEnumerable<Atrai.Core.Entity.ShippingChargeModel>
@{
    ViewData["Title"] = "Shipping Charge List";
    Layout = "~/Views/Shared/_adminLayout.cshtml";
}



<div class="page-wrapper">
    <div class="container-fluid">

        @*

            </div>*@

        <!-- Start Page Content -->


        <div class="card p-1">

            <div class="card-header">
                <div class="row">
                    <div class="col-md-2 col-12">
                        @Html.ActionLink("Create Shipping Charge", "AddShippingCharge", null, null, new { @class = "btn btn-sm btn-outline-info btn-block rounded-0", @id = "btnBack" })
                    </div>
                    <div class="col-md-4 col-12">
                        <h4 style="color: #1976d2;">Shipping Charge List</h4>
                    </div>
                </div>

            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="example23" class="display nowrap table table-sm table-hover  table-bordered table-striped" cellspacing="0" width="100%">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.ShippingLocationName)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.ChargeAmount)
                                </th>

                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {

                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ShippingLocationName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ChargeAmount)
                                </td>
                                <td>
                                    <a href="@Url.Action("EditShippingCharge", "Admin", new { ShippingChargeid = item.Id })" class="btn btn-icon btn-warning rounded-0 btn-sm rounded-0 mb-1">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                        <a href="@Url.Action("DeleteShippingCharge", "Admin", new { ShippingChargeid = item.Id })" onclick="return confirm('Are you sure you want to Remove this ShippingCharge?')" class="btn btn-icon btn-danger rounded-0 btn-sm rounded-0 mb-1">
                                            <i class="fa fa-remove"></i>
                                        </a>
                                </td>

                            </tr>
}
                        </tbody>
                    </table>


                </div>
            </div>
        </div>


        <!-- End PAge Content -->
    </div>
</div>





@section scripts{

}